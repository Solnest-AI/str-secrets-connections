# ============================================================
#  STR Secrets Connections: YOUR KEYS
#  Paste each key after the = sign. No quotes. No spaces.
#  GOOD:  AIRROI_API_KEY=abc123
#  BAD:   AIRROI_API_KEY = "abc123"
#  Save, then tell Claude "saved". NEVER paste a key into the chat.
#  Where each one comes from: KEYS.md
# ============================================================

# ---- Your answers (Claude fills these after asking you once) ----
STACK_PMS=
STACK_PRICING=
STACK_RANKING=
STACK_OPS=

# ---- PMS: fill ONLY the one you use ----
# Hospitable: my.hospitable.com > Apps > API access > Access tokens > + Add new (Read + Write)
HOSPITABLE_API_KEY=
# Hospitable official MCP fallback token (optional; only if the browser sign-in loops, connectors/pms-hospitable.md section 6)
HOSPITABLE_OFFICIAL_TOKEN=
# Hostaway: Settings > Hostaway API > Create (Partner = "Hostaway Public API"); both shown once
HOSTAWAY_ACCOUNT_ID=
HOSTAWAY_API_KEY=
# Guesty Pro: Integrations > OAuth applications > New application; secret shown once
GUESTY_CLIENT_ID=
GUESTY_CLIENT_SECRET=
# Hostfully: Agency Settings > API Key field; Agency UID at bottom of same page
HOSTFULLY_API_KEY=
HOSTFULLY_AGENCY_UID=
# OwnerRez: Settings > Advanced Tools > Developer/API Settings > Personal Access Token (pt_..., shown once) + your login email
OWNERREZ_EMAIL=
OWNERREZ_TOKEN=
# Lodgify: Settings > Public API
LODGIFY_API_KEY=
# Uplisting: Connect > API
UPLISTING_API_KEY=
# Smoobu: Settings > Advanced > API Keys > Create New; secret shown once
SMOOBU_API_KEY=
SMOOBU_API_SECRET=

# ---- Pricing: fill ONLY the one you use ----
# PriceLabs API: app.pricelabs.co/account_settings?tab=api_details > Enable ($1/listing/month)
PRICELABS_API_KEY=
# PriceLabs official MCP (optional, admin only): Account Settings > AI Connector (MCP) > Add Custom Client > Client ID + Client Secret (secret shown once)
PRICELABS_MCP_CLIENT_ID=
PRICELABS_MCP_CLIENT_SECRET=
# Beyond: v2.beyondpricing.com > Settings > Personal Access Tokens (bpat_..., shown once; needs Beyond Pro)
BEYOND_TOKEN=

# ---- Ranking (either, or neither) ----
# RankBreeze: Settings > MCP Credentials > MCP Access > Remote MCP > Copy URL (the whole URL is the key)
RANKBREEZE_MCP_URL=
# IntelliHost: you sign in through the browser; only fill this if that loops (connectors/ranking-intellihost.md section 6)
INTELLIHOST_MCP_TOKEN=

# ---- Ops (optional) ----
# Turno: after Turno grants access: Settings > Turno API > Tokens (eyJ... token) + Partner ID at bottom
TURNO_API_TOKEN=
TURNO_PARTNER_ID=
# TURNO_ENV: production for the summit. Leave blank and Claude sets production.
TURNO_ENV=
# Breezeway: emailed to you by support@breezeway.io
BREEZEWAY_CLIENT_ID=
BREEZEWAY_CLIENT_SECRET=

# ---- Ads (required; you sign in through the browser, this is only a fallback) ----
# Meta Ads: only fill this if the browser sign-in for meta-ads will not stick (connectors/ads-meta.md section 6)
META_ADS_TOKEN=

# ---- Market data (required) ----
# AirROI: airroi.com/api/developer (needs a $10 deposit to activate)
AIRROI_API_KEY=

# ---- AI (required) ----
# Kie: kie.ai/api-key
KIE_API_KEY=
# Gemini: aistudio.google.com/apikey (make a NEW key; do not link billing)
GEMINI_API_KEY=

# ---- Web (required) ----
# Firecrawl: firecrawl.dev/app/api-keys (starts with fc-)
FIRECRAWL_API_KEY=

# ---- Database (required) ----
# Supabase: supabase.com/dashboard/account/tokens > Generate new token > "Create legacy token" link > Expires: Custom, 1 year out (NOT the 7-day default). See KEYS.md.
SUPABASE_ACCESS_TOKEN=
# Claude fills these two; do not edit
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=

# ---- Where your other summit skills live (Claude fills; paths are not secrets) ----
SKILL_PATH_REVENUE_MANAGER=
SKILL_PATH_LISTING_OPTIMIZER=
SKILL_PATH_COMPING_AGENT=
